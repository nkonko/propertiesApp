import { DropZoneDirective } from './drop-zone.directive';

describe('DopZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
